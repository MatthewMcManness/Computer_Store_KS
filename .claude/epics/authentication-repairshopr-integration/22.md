---
name: Add Environment Configuration and Documentation
status: open
created: 2025-11-28T18:35:18Z
updated: 2025-11-28T18:42:25Z
github: https://github.com/MatthewMcManness/Computer_Store_KS/issues/22
depends_on: [15, 16]
parallel: true
conflicts_with: []
---

# Task: Add Environment Configuration and Documentation

## Description

Create comprehensive environment configuration with validation, update `.env.example`, and document the new authentication system.

## Acceptance Criteria

- [ ] Update `.env.example` with all required variables
- [ ] Create environment validation that fails fast on missing config
- [ ] Add `.sessions/` to `.gitignore`
- [ ] Document migration process from legacy to RepairShopr auth
- [ ] Document role mapping and permissions

## Technical Details

### Files to Update/Create

**`.env.example`**
```env
# RepairShopr Authentication
REPAIRSHOPR_SUBDOMAIN=your-subdomain
SESSION_SECRET=generate-32-byte-random-string-here

# Authentication Mode
# Options: 'repairshopr' (default) or 'legacy'
AUTH_MODE=repairshopr

# Legacy Authentication (keep for fallback during migration)
ADMIN_PASSWORD=your-admin-password
```

**`.gitignore` additions**
```gitignore
# Session storage
.sessions/
```

### Environment Validation
Create `src/lib/env.ts`:
```typescript
export function validateAuthConfig() {
  const mode = process.env.AUTH_MODE || 'repairshopr';

  if (mode === 'repairshopr') {
    if (!process.env.REPAIRSHOPR_SUBDOMAIN) {
      throw new Error('REPAIRSHOPR_SUBDOMAIN is required for RepairShopr auth mode');
    }
  }

  if (!process.env.SESSION_SECRET || process.env.SESSION_SECRET.length < 32) {
    throw new Error('SESSION_SECRET must be at least 32 characters');
  }
}

// Call on app startup
validateAuthConfig();
```

### Documentation
Add to README or create `docs/authentication.md`:
- Overview of RepairShopr integration
- Environment setup guide
- Role mapping explanation
- Migration steps from legacy auth
- Troubleshooting common issues

## Dependencies

- [ ] Task 15: RepairShopr client (for config requirements)
- [ ] Task 16: Session store (for session secret requirements)

## Effort Estimate

- Size: S
- Hours: 2
- Parallel: true (can run after 15, 16)

## Definition of Done

- [ ] `.env.example` updated with all variables
- [ ] Environment validation implemented
- [ ] `.sessions/` gitignored
- [ ] Documentation complete
- [ ] Migration guide tested end-to-end
